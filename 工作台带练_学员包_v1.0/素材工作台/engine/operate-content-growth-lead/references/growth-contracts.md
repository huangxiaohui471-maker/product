# 增长岗位最小合同

输入是 `production_to_growth/v1`：当前使用的具体内容版本、目标对象、客户场景、当前变化、使用位置和观察计划。

内容底账的每条记录至少包含作品身份、渠道、发布时间、内容版本、数据截止时间、公开指标、内部指标、业务结果、缺失字段和来源。相同渠道＋作品 ID 更新同一条记录；历史快照追加，不覆盖。

输出是 `growth_learning_return/v1`。每轮结果先作为本轮经验写回；同类经验重复出现时，系统再提高它在下一轮中的参考权重。
