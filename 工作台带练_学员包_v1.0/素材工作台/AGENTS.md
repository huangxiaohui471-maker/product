# 本包执行入口

先读取 SKILL.md 与 README.md。复用 engine 内的上期能力，用户只看当前统一工作台。业务项目保留在独立目录，不把讲师业务或测试数据注入学员项目。

语义判断由当前宿主 Agent 执行；网页请求通过 agent_bridge.py 接续。已保存请求不是任务完成。结果真实写回且在页面可见，才能报告完成。

更新本包时保留业务项目、旧内容版本、采用记录与结果。每次改变只验证受影响路径，不修改并行课件或系统已安装 Skill。
