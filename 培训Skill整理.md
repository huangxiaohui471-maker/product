# 培训 Skill 整理

> 来源：`工作台带练_学员包_v1.0/`，2026-09-19 ~ 09-20 培训
> 共 **7 个 Skill**，分两层：配套Skill（教练层）+ 素材工作台引擎层

---

## 一、配套Skill（教练层）

### 1. build-workbench-loops — 工作台搭建教练

| 项目 | 内容 |
|------|------|
| **版本** | 0.4.4 |
| **一句话** | 带学员三轮改造工作台的教练 Skill |
| **功能** | ① 先确认学员自己的业务（不照抄老师样例）；② 指导"搭出来→试一试→用起来"三轮迭代；③ 每次改动后必须在真实浏览器里操作验证（修改→保存→刷新找回）；④ 控制课堂节奏，不跳轮次 |
| **适用场景** | 学员拿到空白底版，需要 AI 带着一步步搭建/改造自己的工作台时使用。是**培训入口 Skill**，学员说"继续这个工作台"时由它接管 |
| **关键约束** | 没业务资料时只问一个问题就等回答；交付前必须实际浏览器验证；每轮只说一个动作 |

---

## 二、素材工作台引擎层（6个）

### 2. operate-material-workbench — 素材工作台主 Skill

| 项目 | 内容 |
|------|------|
| **版本** | 0.6.0-pilot |
| **一句话** | 整个素材工作台的"前台接待员"，判断用户请求该交给哪个引擎 Skill 处理 |
| **功能** | ① 读取项目状态，不重建已有功能；② 处理页面请求（搜索/下载/拆解→策略；生产/修改→生产；数据整理/复盘→增长）；③ 通过 `agent_bridge.py` 把 AI 结果写回页面；④ 管理交付边界（不自动发布、不编造数据） |
| **适用场景** | 用户在工作台页面上点了某个操作、或说"处理页面里的请求"时使用。是**总调度入口** |
| **关键约束** | 网页不自己调模型，由宿主 Agent 执行后桥接回页面；等待时如实显示，不假装后台在跑 |

---

### 3. operate-content-strategy-lead — 内容策略负责人

| 项目 | 内容 |
|------|------|
| **一句话** | 负责"发现+判断"：从公开内容中找方向，产出今日推荐策略 |
| **功能** | ① 企业 onboarding：读资料生成《企业人货场白皮书》；② 通过 TikHub 采集公开自然流内容（可选飞瓜付费素材）；③ 视频拆解（关键帧+转写+OCR）；④ 去重、打标签、建素材库；⑤ 产出"今日一个方向"（单变量实验任务）；⑥ 渲染策略看板；⑦ 导出 `strategy_to_production/v1` 交给生产负责人 |
| **适用场景** | ① 新企业接入，需要建白皮书；② 每天找"今天发什么"；③ 看到竞品/爆款想判断适不适合自己；④ 策略回流后重新定方向 |
| **关键约束** | 公开热度 ≠ 投放效果；借机制不抄执行；每次只改一个变量；缺证据就降级不编造 |

---

### 4. operate-content-production-lead — 内容生产负责人

| 项目 | 内容 |
|------|------|
| **一句话** | 负责"生产"：把策略方向做成可直接使用的成套内容 |
| **功能** | ① 接收 `strategy_to_production/v1`，锁定策略任务和素材引用；② 按渠道自动选生产线（销售文章/短视频/营销图片）；③ 生成可用版本（默认一个最推荐，必要时2-3候选）；④ 图片套装围绕同一主张分工；⑤ 管理版本（首版→反馈→新版，不覆盖旧版）；⑥ 生成 `production_to_growth/v1` 交给增长负责人 |
| **适用场景** | 策略已定，需要"把这个方向做成内容"——写脚本、做图、出方案 |
| **关键约束** | 不能重新猜客户/场景/主张；视频路线未达稳定门，不承诺成片；事实缺口如实说 |

---

### 5. operate-content-growth-lead — 内容增长负责人

| 项目 | 内容 |
|------|------|
| **一句话** | 负责"触达+复盘+升级"：让内容到达客户，回收结果，把学习变成下一轮动作 |
| **功能** | ① 接收 `production_to_growth/v1`，建立触达记录；② 导入作品数据（抖音优先企业号链接），更新内容底账；③ 批量诊断（事实→观察→解释→假设→行动的推论阶梯）；④ 输出业务决定：继续/修改/停止/再验证；⑤ 生成 `growth_learning_return/v1` 交回策略负责人 |
| **适用场景** | ① 内容已发布，需要回收数据做复盘；② 导入账号历史作品建基线；③ 决定下一轮内容方向（保留什么/改变什么） |
| **关键约束** | 公开互动 ≠ 成交结论；没有可比基线不说"高于正常"；停止必须有事实依据 |

---

### 6. operate-ai-content-flywheel — AI内容飞轮总控

| 项目 | 内容 |
|------|------|
| **一句话** | 跨岗位编排器：判断任务该进哪一环，自动接棒、处理等待、生成下一轮 |
| **功能** | ① 从任一环进入（发现/判断/生产/触达/复盘/升级）；② 每次只派一个负责人；③ 交接检查（同企业、同任务、文件可用）；④ 更新唯一 `task_state.json`；⑤ 渲染驾驶舱和部门首页；⑥ 支持离场运行（定时/条件触发）；⑦ 生成自动化周期计划 |
| **适用场景** | ① 老板说"帮我找今天发什么"（自动路由到策略）；② 跨岗位任务需要编排；③ 需要驾驶舱看全局状态；④ 配置"每天找素材、每周复盘"自动化 |
| **关键约束** | 不替岗位做专业判断（不自己写脚本/做图/解释数据）；只显示一个当前动作；不把等待显示为运行中 |

---

### 7. kimi-webbridge — 浏览器控制桥

| 项目 | 内容 |
|------|------|
| **版本** | 1.11.5 |
| **一句话** | 让 AI 控制用户真实浏览器的工具 Skill |
| **功能** | ① 导航/点击/填表/截图/读页面；② 利用用户已登录的浏览器会话；③ 网络请求抓取；④ 页面存PDF；⑤ 多标签页分组管理 |
| **适用场景** | ① 需要操作网页（登录态下）；② 抓取网页内容/截图；③ 飞瓜等需要浏览器登录的平台采集；④ 页面验收（配合 build-workbench-loops 的浏览器验证要求） |
| **关键约束** | 严格检查 `isTrusted` 的站点（银行/验证码）无法自动化；跨域 iframe 需直接导航到 iframe URL |

---

## 三、Skill 之间的关系

```
build-workbench-loops（教练）
    │
    ▼
operate-material-workbench（前台调度）
    │
    ├──► operate-content-strategy-lead（发现+判断）
    │         │
    │         ▼ strategy_to_production/v1
    │
    ├──► operate-content-production-lead（生产）
    │         │
    │         ▼ production_to_growth/v1
    │
    ├──► operate-content-growth-lead（触达+复盘）
    │         │
    │         ▼ growth_to_strategy.json（回流）
    │
    └──► operate-ai-content-flywheel（总控编排，跨岗位调度）
              │
              └──► kimi-webbridge（浏览器操作工具，被以上各 Skill 调用）
```

---

## 四、快速选用表

| 你想做什么 | 用哪个 Skill |
|-----------|-------------|
| 开始搭建/改造工作台 | `build-workbench-loops` |
| 处理工作台页面上的请求 | `operate-material-workbench` |
| 找方向、判断竞品、定策略 | `operate-content-strategy-lead` |
| 把方向做成内容（脚本/图片） | `operate-content-production-lead` |
| 复盘已发布内容的效果 | `operate-content-growth-lead` |
| 跨岗位编排、看全局状态 | `operate-ai-content-flywheel` |
| 操作浏览器、抓网页 | `kimi-webbridge` |
