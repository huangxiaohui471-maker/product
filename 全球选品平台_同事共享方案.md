# 全球选品平台 · 同事共享方案

> 适用版本：V12（当前公开发布版本）
> 方案日期：2026-09-21

## 一、先说结论

- **让同事「能看」→ 现在就能做，零成本。** 直接把公开链接发给他，不需要账号、不需要授权，打开就是完整的看板 / 榜单 / 筛选和全部云端数据（已实测）。
- **让同事「能一起操作」→ 需要两步**：把资产搬进一个团队空间，然后把同事加为成员。第一步我可以代做；第二步平台没有开放接口，需要你在界面上点一次「邀请」。
- 如果你希望同事**完全没有 WorkBuddy 账号也能拿到一份可存档的完整版**，我可以再生成一个「离线快照单文件」，双击即看。

---

## 二、现状盘点（已实际查证，非推测）

所有资产目前都在**「我的文档」个人空间**里，所有者只有你（惠子）一人，**尚无任何其他协作成员**。

| 资产 | 类型 | 链接 | 当前协作者 |
|---|---|---|---|
| 全球新品选品台（主平台） | 页面 | `workbuddy.cn/space/d/lMO9EIAM8o5cIwqudwUDr1` | 仅你（owner） |
| └ 全球新品库（42 字段 / 400 条） | 数据表 | `workbuddy.cn/space/d/Hu5q2PAyW17BmdP5JPQ9os` | 仅你（owner） |
| 自有店铺口碑库（21 字段 / 37 条） | 数据表 | `workbuddy.cn/space/d/ZOiFonS5w7psZlgM5kJjiD` | 仅你（owner） |
| 美妆情报台 | 页面 | `workbuddy.cn/space/d/1UxG6gd1n5I7NTYBv2daGs` | 仅你（owner） |

页面 ↔ 数据表关联关系**已建立**（主平台同时挂在「全球新品库」和「自有店铺口碑库」两张表上），所以同事打开页面时数据是通的。

---

## 三、关键实测结果（这决定方案怎么选）

我在**全新浏览器（等于未登录访客）**里打开了公开链接，结果如下：

| 探测项 | 结果 | 说明 |
|---|---|---|
| 公开链接 HTTP 状态 | **200** | 正常可访问 |
| 页面内是否拿到云端数据通道 | **是** | 平台对发布页开放了只读数据通道 |
| 实际读取到的记录数 | **400 条** | 完整云端数据，不是示例数据 |
| 页面渲染 | 正常 | 看板 / 榜单 / 筛选全部可用 |
| 控制台报错 | 0 | — |

同时确认：**同事直接在本地双击 HTML 文件打开是不行的**——那种情况下拿不到平台通道，页面会退化成离线模式，只剩 17 条示例数据。

**结论**：公开链接这条路是真实可用的，不是「理论上可以」。

页面本身还具备**写入能力**（可新增 / 删除记录），并且接入了**数据变更订阅**——也就是说，一旦同事有了编辑权限，他改数据你能实时看到，反之亦然。但这些写操作在没有权限时会被平台拦截，所以「一起操作」必须走协作权限。

---

## 四、三条路径对比

| 维度 | A 公开链接（只读） | B 团队空间协作（可操作） | C 离线快照单文件 |
|---|---|---|---|
| 同事需要 WorkBuddy 账号 | 不需要 | **需要** | 不需要 |
| 能否看到数据 | 能，实时读云表 | 能，实时读云表 | 能，但是快照 |
| 能否改数据 / 回填字段 | 不能 | **能**（表内任意字段 + 页面新增删除） | 不能 |
| 双向实时同步 | — | **有** | 无 |
| 数据外泄风险 | **较高**（拿到链接即可读全部） | 低（仅授权成员） | 中（文件可被转发） |
| 落地成本 | 零，立刻可用 | 约 10 分钟 + 一次界面点击 | 几分钟（我生成） |
| 适合场景 | 只关心结论的人、临时演示 | 日常一起选品、回填、复盘 | 存档、外发、离线看 |

---

## 五、推荐做法

**B 为主 + A 为辅**：日常协作走团队空间（同事有账号、能改数据、实时同步）；对外或临时给人看，直接甩公开链接。

C 作为备选，只在同事没有 WorkBuddy 账号、或需要一份可存档的完整版时才做。

---

## 六、执行步骤

### 第 1 步【我代做】建团队空间并归置资产

- 新建团队空间（建议命名：**全球选品平台**）
- 把主平台、两张数据表、美妆情报台搬进去，并建好目录层级
- 搬完复检：公开链接仍可访问、页面数据仍正常读取

> 这一步全部通过接口完成，你不需要动手。

### 第 2 步【你手点一次】邀请同事加入

平台**没有开放「添加协作者」的接口**（只能查询，不能添加），所以这一步必须你在界面上完成：

1. 打开 `https://www.workbuddy.cn/space/d/lMO9EIAM8o5cIwqudwUDr1`
2. 点右上角的**分享 / 成员**入口
3. 输入同事的账号（邮箱或手机号），角色给**编辑**（要让他能改数据）或**管理员**
4. 发送邀请

> 如果界面里个人空间的节点也支持直接加协作者，那就不用搬空间，第 1 步可以跳过——你点开看一眼就知道。

### 第 3 步【同事做】用自己账号打开

可以直接把下面这段发给同事：

> 打开 https://www.workbuddy.cn/space/d/lMO9EIAM8o5cIwqudwUDr1
> 看板 / 榜单 / 筛选都可以直接用；要改数据就打开「全球新品库」这张表直接编辑，改完我这边页面会自动刷新。

---

## 七、注意事项与风险

1. **公开链接 = 任何人拿到就能读全部数据。** 里面有你的选品判断和成本信息，如果要收回，随时可以取消发布（链接即刻失效）。
2. **搬动节点不会影响公开链接**，公开链接是按节点 ID 走的；但搬完我会复检一次。
3. **官方规则确认**：资料库里「分享」和「发布」是两件事——协作态链接本身就可以分享给成员，发布是另一个独立动作。所以给同事用协作链接即可，不必重新发布。
4. **取数能力不建议整包交接**。同事若也要自己跑抖音罗盘 / 蝉妈妈 / FastMoss / TikTok 取数，会牵涉到你的登录态和 API key，不适合直接给。建议先按「你出数、他看数改数」的模式跑一段，确实需要再单独设计分工。
5. **同事需要能改数据才有意义**——如果只给「查看」角色，他在页面上仍然改不动，等于也是只读。

---

## 八、可选升级（有需要再说）

- **离线快照单文件**：把 400 条云表数据注入页面种子，产出一个自包含 HTML，双击即看、可存档、可微信/邮件转发（缺点是数据不会更新）。
- **同事版口径说明**：给他一份字段口径表 + 使用说明，避免他误读「环比增速」「榜单样本占比」这些口径。
- **权限分级**：核心的「选品笔记 / 决策状态」字段单独控权，避免误改。
